<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">Monitoring serre et champ maraîchage</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('home') }}"
                       class="nav-link {% if current_menu is defined and current_menu == 'home' %}active{% endif %}">
                        <i class="fa fa-home"></i> Accueil </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('tab') }}"
                       class="nav-link {% if current_menu is defined and current_menu == 'tab' %}active{% endif %}">
                        <i class="fa fa-table"></i> Tableaux </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('graph') }}"
                       class="nav-link {% if current_menu is defined and current_menu == 'graph' %}active{% endif %}">
                        <i class="fa fa-area-chart"></i> Graphiques </a>
                </li>
            </ul>
        </div>
    </div>
</nav>